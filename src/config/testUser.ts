// TEMPORARY: there's no login/signup flow yet, so every booking is made as
// this one hardcoded test user (a real row in public.users, backed by a real
// auth.users row). Replace with the signed-in user's id once auth exists —
// search the codebase for TEST_USER_ID to find every place this is used.
export const TEST_USER_ID = 'e8c1d7e3-3d6c-490f-b995-b4e98e2b5efa';
